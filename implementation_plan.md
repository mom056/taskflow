# خطة تطوير TaskFlow v2 الشاملة

هذه هي الخطة التنفيذية لتطوير التطبيق وإضافة الميزات المتقدمة. الخطة مقسمة إلى 4 مراحل متتالية:

---

## 🛠️ المرحلة الأولى: الأداء والجودة التقنية (تحسين فوري)

### 1. ضغط الصور تلقائياً (Image Compression)
* **المشكلة:** يرفع الموظفون صوراً بحجم 5-10 ميجابايت مما يستهلك سعة التخزين والإنترنت.
* **الحل:** دمج مكتبة `browser-image-compression` لضغط الصور تلقائياً في المتصفح قبل رفعها لـ Supabase لتصبح أقل من **500 كيلوبايت**.
* **الواجهة:** إضافة مؤشر ذكي للموظف: `"جاري ضغط الصورة..."` ثم `"جاري الرفع..."`.

### 2. دعم العمل دون اتصال بالإنترنت (PWA & Offline Cache)
* **المشكلة:** يفقد المندوب التغطية في بعض المواقع الميدانية فلا يستطيع تحديث المهام.
* **الحل:** 
  * تحويل التطبيق لتطبيق ويب تقدمي (PWA) قابل للتثبيت على الهاتف عبر مكتبة `vite-plugin-pwa`.
  * تخزين التحديثات في ذاكرة المتصفح `localStorage` عند انقطاع الشبكة وإرسالها فور عودة الإنترنت.
  * إظهار شريط تنبيه ذكي: 🔴 *"أنت غير متصل - سيتم حفظ تعديلاتك ومزامنتها لاحقاً"*.

---

## 🛰️ المرحلة الثانية: الميزات الميدانية والتحقق (Field Verification)

### 1. التحقق الجغرافي (GPS & Geofencing)
* **الهدف:** التأكد من تواجد الموظف في الموقع الفعلي للمهمة عند الضغط على "مكتملة".
* **الحل:**
  * التقاط إحداثيات الموظف (Latitude & Longitude) عبر الـ GPS المدمج في الهاتف عند إرفاق الصورة أو تحديث الحالة.
  * حفظ هذه البيانات في جدول المهام في قاعدة البيانات.
  * إضافة خريطة تفاعلية للمدير في لوحة التحكم تعرض نقاط الزيارات للموظفين.

### 2. الإشعارات الفورية (Push Notifications)
* **الهدف:** تنبيه الموظف على هاتفه فوراً عند إسناد مهمة جديدة له.
* **الحل:** برمجة إشعارات الويب (Web Push API) وربطها بـ Supabase Edge Functions لإرسال إشعار فوري للموظف حتى لو كان التطبيق مغلقاً.

---

## 📊 المرحلة الثالثة: التقارير والتحليلات الذكية (Reporting)

### 1. لوحة التحليلات والرسوم البيانية (Manager Analytics)
* **الهدف:** تمكين المدير من قراءة أداء الفريق بدقة.
* **الحل:** استخدام مكتبة الرسوم البيانية `Recharts` لعرض:
  * رسم بياني خطي لمعدل إنجاز المهام الأسبوعي مقارنة بالأسبوع السابق.
  * رسم بياني عمودي لمقارنة أداء الموظفين ونسبة إنجازهم.
  * مؤشرات أداء رقمية (متوسط وقت الإنجاز، الموظف الأكثر نشاطاً).

### 2. تصدير التقارير بصيغة PDF
* **الهدف:** طباعة تقارير الزيارات والمهام للمشاركة الخارجية.
* **الحل:** استخدام مكتبة `jspdf` لإنشاء ملفات PDF منسقة تحتوي على شعار الشركة وجدول الزيارات وصور المواقع المرفوعة.

---

## 👤 المرحلة الرابعة: إدارة الحساب والملف الشخصي (Settings)

### 1. صفحة إعدادات الموظف والمدير
* **الهدف:** تعديل البيانات الشخصية بأمان.
* **الحل:**
  * تمكين الموظف من تغيير كلمة مروره وتعديل اسمه ورفع صورة شخصية (Avatar).
  * تمكين المدير من إضافة موظفين جدد مباشرة من داخل التطبيق دون الحاجة لدخول لوحة تحكم Supabase.
